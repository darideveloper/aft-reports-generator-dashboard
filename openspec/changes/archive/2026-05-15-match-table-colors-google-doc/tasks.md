## 1. Style Implementation

- [x] 1.1 Update `th` background color in `survey/templates/survey/pdf/style.css` to `#d9e2f3`.
- [x] 1.2 Update `.indicators-table` header and column colors in `survey/templates/survey/pdf/style.css`.

## 2. Verification

- [x] 2.1 Verify the PDF preview matches the Google Doc colors.
