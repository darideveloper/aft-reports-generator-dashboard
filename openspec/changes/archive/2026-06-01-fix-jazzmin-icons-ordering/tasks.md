## 1. Jazzmin Configuration

- [x] 1.1 Update `JAZZMIN_SETTINGS["icons"]` in `project/settings.py` with new icons and standardized keys.
- [x] 1.2 Update `JAZZMIN_SETTINGS["order_with_respect_to"]` in `project/settings.py` with the complete list of models in the correct order.

## 2. Verification

- [x] 2.1 Verify that the Django server starts without errors.
- [x] 2.2 Manually verify the icons and ordering in the Django admin sidebar.
