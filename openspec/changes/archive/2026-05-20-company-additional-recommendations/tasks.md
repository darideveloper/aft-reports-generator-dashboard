- [x] **Add database field:** Add `additional_recommendations` (`TextField`, blank=True, null=True, verbose_name="Recomendaciones Adicionales", help_text="Recomendaciones personalizadas para el reporte grupal en PDF. Una por línea.") to the `Company` model in `survey/models.py`.
- [x] **Generate migrations:** Run `python manage.py makemigrations survey` to create the migration for the new field.
- [x] **Update view logic:** Modify `GroupReportPDFView` in `survey/views.py` (around line 206) to replace the static `additional_recommendations` list. Instead, parse `company.additional_recommendations` (split by newlines and strip whitespace, ignoring empty lines) and pass the resulting list into the template context. If the field is empty, pass an empty list.
- [x] **Update PDF template:** Modify `survey/templates/survey/pdf/group_report_template.html` (under `<!-- Page 12 -->`). Wrap the "10. Recomendaciones adicionales" `<h2>` and the `.text-content` div containing the loop with `{% if additional_recommendations %}` ... `{% endif %}`.
- [x] **Ensure fixed text visibility:** Verify in the template that the "Mensaje de LeadForward Global Solutions MJ" section (the `.lf-message` div) remains *outside* the `{% if additional_recommendations %}` block so it is always rendered.